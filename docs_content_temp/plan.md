# 451-docs Project Plan (Summary)

Last updated: 2026-04-11

## Objective

Run a lightweight notes site where Markdown content stays in a private repository and the public site serves content at runtime through Netlify Functions.

## Current architecture baseline

- Public and protected posts are fetched from `Shoei451/md-contents` via GitHub Contents API.
- App source lives under `src/` and Netlify publishes a built `dist/` output generated by `scripts/build.js`.
- `post.html?slug=` is the single public post viewer and `protected-post.html?slug=` is the single password-gated viewer.
- Site variants are selected with `?site=` and resolved through `netlify/functions/_lib/config.js`.
- Password checks for protected posts happen on the server (`/api/protected-post`), not in browser-only logic.
- Per-post optional libraries (`katex`, `highlight`) are loaded dynamically from frontmatter flags.
- Utility pages live under `src/toolbox/`.

## What is complete

- Runtime API delivery for public posts.
- Runtime API delivery for protected posts.
- Shared helper layers for frontmatter parsing, GitHub access, and CORS.
- Unified home feed rendering for public and protected metadata.
- Source-tree reorganization into `src/` plus deployable `dist/` build output.
- Repo-local validation scripts for JavaScript syntax and local links.
- Multi-site UI/accent configuration and category/search filtering on the home feed.

## What remains

- RSS feed support.
- Further JS/CSS consolidation after feature stabilization.

## Source-of-truth links

- Execution status and priorities: `docs/roadmap.md`
- Completed history: `docs/changelog.md`
- Quick capture inbox: `docs/todo.md`
